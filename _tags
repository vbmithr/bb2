true: debug, bin_annot, safe_string

<src>: include

<src/*.*>: package(core), \
           package(async), \
           package(ocamlgraph), \
           package(bittrex.async), \
           package(ppx_deriving.std), \
           package(containers), \
           thread
